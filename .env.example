# Какую БД использовать: local / remote
USE_DB=local

# Локальная БД (Docker)
DB_HOST=localhost
DB_PORT=5434
DB_NAME=mock_uto
DB_USER=kmg_user
DB_PASSWORD=CHANGE_ME_STRONG_PASSWORD

# Удалённая БД (хакатон, readonly)
REMOTE_DB_HOST=
REMOTE_DB_PORT=5432
REMOTE_DB_NAME=mock_uto
REMOTE_DB_USER=readonly_user
REMOTE_DB_PASSWORD=

# API
API_HOST=0.0.0.0
API_PORT=8003

# LLM (опционально, для генерации обоснований на естественном языке)
GEMINI_API_KEY=